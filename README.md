# silhouette
Silhouette is a local service which reconstructs the observed architecture and request flow of a system from telemetry so users can see how requests move through it, where observability breaks down, and eventually where architectural weaknesses and latency concentrate
